#include "main.h"
/**
*_abs - a function gives last digit form SRAND
*@n: the int value
*Return: 1 if c is lowercase and 0 otherwise
*/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * (-1));
	}
}
